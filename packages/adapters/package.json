{
  "name": "@lottery/adapters",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --root ../../ --config ../../vitest.config.ts packages/adapters/test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lottery/contracts": "workspace:*",
    "@lottery/domain": "workspace:*"
  }
}
